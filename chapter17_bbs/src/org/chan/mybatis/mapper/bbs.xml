<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"https://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="org.chan.mybatis.mapper.bbs">
	<!-- 모든 게시글 보기 -->
<!--	<select id="bbs_select_all" resultType="org.chan.vo.BVO">
		SELECT * FROM bbs_t ORDER BY b_idx DESC
	</select> -->
		
	<!-- 페이징 게시글 -->
	<select id="bbs_select_all_with_paging" resultType="org.chan.vo.BVO">
		<![CDATA[
			SELECT b_idx, writer, title, hit, reg_date 
			FROM
				(SELECT ROWNUM rn, a.* 
				FROM 
					(SELECT b_idx, writer, title, hit, reg_date FROM bbs_t ORDER BY b_idx DESC) a 
				WHERE ROWNUM <= #{pageNum} * #{amount})
			WHERE rn > (#{pageNum}-1) * #{amount}
		]]>
	</select>
	
	<!-- 게시글 전체 수 가져오기 -->
	<select id="total_count_of_bbs" resultType="int">
		SELECT COUNT(b_idx) FROM bbs_t
	</select>
	
	<!-- 게시글 작성 -->
	<insert id="insert_bbs" parameterType="org.chan.vo.BVO">
		INSERT INTO bbs_t VALUES (bbs_seq.nextval, #{writer}, #{title}, #{content}, #{pw}, 0, #{ip}, #{filename}, sysdate)
	</insert>
	
	<!-- 게시글 내용 보기 -->
	<select id="bbs_by_idx" parameterType="int" resultType="org.chan.vo.BVO">
		SELECT * FROM bbs_t WHERE b_idx = #{b_idx}
	</select>
	
	<!-- 게시글 삭제 -->
	<delete id="delete_bbs">
	 	DELETE FROM bbs_t WHERE b_idx = #{b_idx}
	</delete>
	
	<!-- 게시글 수정 -->
	<update id="update_bbs" parameterType="org.chan.vo.BVO">
		UPDATE bbs_t SET title = #{title}, content = #{content}, filename = #{filename} WHERE b_idx = #{b_idx}
	</update>
	
	<!-- 조회수 증가 -->
	<update id="update_hit" parameterType="org.chan.vo.BVO">
		UPDATE bbs_t SET hit = #{hit} WHERE b_idx = #{b_idx} 
	</update>
</mapper>